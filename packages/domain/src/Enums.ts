export enum TaskFrequency {
  Daily = "daily",
  Weekly = "weekly",
  Monthly = "monthly",
  OneTime = "one-time",
}

export enum TaskActivityStatus {
  Completed = "completed",
  Failed = "failed",
}

export enum ActivityType {
  Task = "task",
  LevelUp = "levelUp",
  Reward = "reward",
}
