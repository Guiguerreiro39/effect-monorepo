export enum TaskFrequency {
  Daily = "daily",
  Weekly = "weekly",
  Monthly = "monthly",
  OneTime = "one-time",
}

export enum TaskCompletionStatus {
  Pending = "pending",
  Completed = "completed",
  Failed = "failed",
}
